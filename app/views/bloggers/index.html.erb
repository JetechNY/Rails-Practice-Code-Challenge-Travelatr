<fieldset>
<legend>Bloggers</legend>
<%  @bloggers.each do |blogger| %>
<p> <%= link_to blogger.name, blogger_path(blogger) %> </p>
<% end %>
</fieldset>
<%= button_to "Create Blogger", new_blogger_path, method: :get%>